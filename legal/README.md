# Legal drafts (archive)

**Canonical published copies** live in [`../docs/`](../docs/) (`privacy/`, `terms/`, `support/`).

Update the `docs/` HTML when policies change, then redeploy hosting. These root-level HTML files are kept for reference and may lag the docs folder.
